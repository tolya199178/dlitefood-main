#map_canvas
  select(ng-model="selectedDriver", ng-change="selectDriver()")
    option(value="0") Select Staff
    option(ng-repeat="driver in drivers", value="{{driver.staff_id}}") {{driver.staff_name}}
  map(zoom="3" center="40.740, -74.18")
    street-view-panorama(
      click-to-go="true",
      disable-default-u-i="false",
      disable-double-click-zoom="false",
      visible="{{showStreetMap}}")
  